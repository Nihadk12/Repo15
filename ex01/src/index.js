let nyName;
let message;

myName = "Padawans";

function myMsg(name) {
    "use struct";
     message = "Hello, " + name + "!";

return message;
}

console.log(myMsg(myName));
module.exports = myMsg;
